//import * from './math/polyfills.js'
import * as v from './math/vectorsv2.js'
/**
class Simulation {
    constructor(worldWidth, worldHeight, worldDepth) {
        this.tick = 0;
    }
    addObject(SimObject) {

    }
    #updateSim() {
        for ()
    }
    tick(time) {
        this.tick = time;
        this.#updateSim();
    }
}

const diffVelocity = v.subtractVectors(v.Vec2(2,1), v.Vec2(2,3));
const accelaration = v.
const v1 = v.Vec1(23);
**/